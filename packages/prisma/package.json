{
  "name": "@repo/prisma",
  "version": "1.0.0",
  "main": "./index.ts",
  "scripts": {
    "migrate": "bun migrate:valledupar && bun migrate:cuestecitas",
    "migration": "bunx prisma migrate dev --schema ./temp/merged-schema.prisma",
    "merge": "bun  ./merge",
    "migrate:valledupar": "bun merge valledupar.prisma && bun migration",
    "migrate:cuestecitas": "bun merge cuestecitas.prisma && bun migration"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2"
  },
  "devDependencies": {
    "prisma": "^6.17.1"
  }
}
